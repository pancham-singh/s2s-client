# import Course from '../Course/schema.graphql'
# import Topic from '../Topic/schema.graphql'

type Module {
  id: ID!
  name: String!
  description: String
  icon: String
  coverImage: String
  course: Course
  topics: [Topic]
}
